services:
  app:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./app:/app
      - ./data:/data
    restart: unless-stopped
    command: bash -lc "pip install -r requirements.txt && python main.py --loop"

